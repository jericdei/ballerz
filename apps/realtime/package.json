{
  "name": "realtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/api": "*",
    "@repo/db": "*",
    "@repo/shared": "*",
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "@types/bun": "latest",
    "typescript": "5.9.2"
  }
}
